spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/my_db
    username: bakdaulet
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect